/**
 * @deprecated Importe de @/shared/types em vez de @/types/inputs
 * Este arquivo será removido em versões futuras.
 */

export type {
  FormEntryType,
  PhaseInputType,
  TodoInputType,
} from '@/shared/types/inputs';

export { isValidFormEntry, isValidPhaseInputType, isValidTodoInputType } from '@/shared/types/inputs';
