/**
 * SavedTodosPanel - Re-exportação do componente refatorado
 *
 * O componente foi refatorado e movido para:
 * ./saved-todos/SavedTodosPanel.tsx
 *
 * Esta versão é mantida para compatibilidade com imports existentes.
 *
 * Estrutura refatorada:
 * - saved-todos/
 *   - SavedTodosPanel.tsx (componente principal ~370 linhas vs 990 original)
 *   - hooks/
 *     - useTodoFiltering.ts (lógica de filtragem)
 *     - useTodoDragDrop.ts (lógica de drag & drop)
 *     - useTodoTouch.ts (gestos de toque)
 *   - components/
 *     - TodoViewButtons.tsx (botões de visualização)
 *     - TodoPanelHeader.tsx (header do painel)
 *     - TodoPagination.tsx (controles de paginação)
 *     - TodoModeButtons.tsx (botões de modo)
 */

export { SavedTodosPanel } from './saved-todos';
