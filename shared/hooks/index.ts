/**
 * Hooks genéricos compartilhados
 * @module shared/hooks
 */

export { useDataCache, createCacheStore } from './useDataCache';
export type { CacheEntry, CacheOptions } from './useDataCache';
// TODO: Add hooks here when needed
export const sharedHooks = {};
